API Reference
=============

The two classes a consumer imports. Everything else in the package is
either shared plumbing or the simulator, which is a test tool
documented in prose in ``docs/simulator.md``.

For what the door *does*, read ``operation.md``; for what goes over the
wire, ``protocol.md``. This reference is the signatures.

.. toctree::
   :maxdepth: 2

   door
   client
